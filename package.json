{
  "name": "grainary",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "cd store && npm run dev",
    "build": "cd store && npm install && npm run build",
    "start": "cd store && npm start",
    "lint": "cd store && npm run lint"
  },
  "dependencies": {
    "next": "16.2.9"
  }
}
